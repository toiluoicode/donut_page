import 'package:flutter/material.dart';
class PanacakeTab extends StatelessWidget {
  const PanacakeTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("Panacake Tab")),
    );
  }
}
